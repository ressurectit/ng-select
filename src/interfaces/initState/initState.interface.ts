/**
 * Wraps state of initialization
 */
export interface InitState
{
    //######################### properties #########################

    /**
     * Represents state of initialization
     */
    initialized: boolean;
}
